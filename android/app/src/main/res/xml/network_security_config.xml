<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is permitted only for private-range addresses. The normal path is a
  Cloudflare tunnel over HTTPS; this exists so a debug build can talk to the
  gateway directly on the LAN or through the emulator's host alias without
  disabling TLS enforcement everywhere.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
